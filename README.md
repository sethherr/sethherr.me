### :cake: [sethherr.me](http://sethherr.me) 

I have a simple bash profile on here, to help myself and others :boom:

Pull it down and set it up:

```bash
curl -o ~/.bash_profile http://sethherr.me/bash_profile && source ~/.bash_profile
```
